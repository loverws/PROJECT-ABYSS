-- Mobile input configuration
-- Provisional/tunable values, frozen=false
-- Values remain Human-Gate tunable
return {
    frozen = false,
    aimAssistStrength = 0,
    aimAssistMax = 0.5,
    lookSensitivity = 1.0,
    -- No target data
}
